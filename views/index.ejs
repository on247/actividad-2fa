<!DOCTYPE html>
<html>
  <head>
    <title>Perfil</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Hola <%= locals.user.name %></h1>
    <p>Ultimo inicio de sesion: <%= locals.user.lastlogindate %> </p>
    <h3>Editar perfil</h3>
    <form action="/" method="POST">
      <label>Nombre</label>
      <input type="text" name="name" id="name"/>
      <input type="submit"/>
      <br/>
      <a href=/logout>Cerrar sesion</a>
      <% if (locals.msg) { %>
        <p><%= locals.msg %> </p>
    <% } %></html>
  </form>
  </body>
  
</html>
